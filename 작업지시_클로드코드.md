# Claude Code 작업 지시문 모음

`~/dev/data_test`에서 `claude` 실행 후 아래를 통째로 붙여넣으세요.
`CLAUDE.md`가 같은 폴더에 있으면 프로젝트 맥락은 자동으로 읽힙니다.

작업은 순서대로 진행하는 걸 권합니다. 앞 작업이 뒤 작업의 전제입니다.

---

## 작업 1 — 프롬프트 튜닝 루프 (지금 바로)

> v0 에이전트의 답변 품질을 평가셋으로 반복 측정하며 개선해줘.
>
> 현재 상태: `evalset_v2.json` 22문항(coverage≠none) 기준 정답률 77.3%.
> 검색 계층 천장은 88.9%라서, 그 차이가 전부 Composer 프롬프트에서 나고 있어.
> 실패 문항의 근거는 이미 검색 상위 5개 안에 들어와 있었어. 검색은 건드리지 마.
>
> 이렇게 진행해줘.
>
> 1. 먼저 현재 점수를 측정해:
>    `python eval_answers.py --adapter agent:answer_for_eval --evalset evalset_v2.json --out base.json`
> 2. ❌로 나온 문항의 답변 원문을 `base.json`에서 읽어서, 왜 틀렸는지 분류해.
>    - (a) 근거는 있는데 세부 수치·조건을 빠뜨림
>    - (b) 답할 수 있는데 "확인되지 않습니다"로 거부함 (과잉 거부)
>    - (c) 근거의 결론을 반대로 냄
>    - (d) 답은 맞는데 채점기 문자열이 안 맞음 (오답 아님)
> 3. (d)는 `build_evalset_v2.py`에서 `answer_points`에 `|`로 대안 표현을 추가해서
>    고쳐. 답변이 실제로 맞는지 근거 청크와 대조하고 나서 고쳐야 해.
>    `python build_evalset_v2.py`로 재생성.
> 4. (a)(b)(c)는 `agent.py`의 `SYSTEM_PROMPT`를 수정해서 대응해.
>    **한 번에 한 가지만 바꾸고** 매번 재측정해. 무엇이 효과 있었는지 알아야 해.
> 5. 개선/악화된 문항을 매 라운드 기록해줘. 이전 라운드에서 맞던 게 틀리면
>    (회귀) 즉시 알려줘. 실제로 규칙을 강화했다가 3문항이 회귀한 적이 있어.
>
> 목표는 85% 이상. 3~4라운드 돌려보고 더 안 오르면 멈추고 결과를 정리해줘.
> `evalset_v1.json`(40문항)으로도 한 번 돌려서 두 셋의 점수를 비교해줘.
>
> 주의: `--show`로 답변 원문을 꼭 확인해. 채점기는 문자열 매칭이라 정답을 오답으로
> 잡는 경우가 있어. 숫자만 보고 판단하지 마.

---

## 작업 2 — Fee-SQL 노드

> `agent.py`에 Fee-SQL 노드를 추가해줘. 지금은 검색만 있어서 "총보수 0.5% 이하
> 펀드 목록" 같은 수치 비교·정렬 질문을 못 풀어. 벡터 검색으로는 원리상 불가능해.
>
> 요구사항:
>
> 1. `route()`가 SQL이 필요한 질의를 판별하도록 확장해.
>    신호: 수치 비교("이하", "이상", "미만", "초과"), 정렬("가장 싼", "낮은 순"),
>    집계("평균", "몇 개"), 그리고 `총보수`·`수수료`·`보수` 같은 필드 키워드.
>    판단은 규칙으로 해. LLM 호출 1회를 아끼면 데드라인에 여유가 생겨.
>
> 2. `fee_sql(state)` 노드를 만들어. 자연어 → SQL 변환은 **HCX-007**로 해야 해
>    (대회 규정상 에이전트 워크플로우의 LLM은 HyperCLOVA 계열 필수).
>    `call_hcx()`를 재사용하고, 프롬프트에 `fund_fees` 테이블 스키마와 실제 값
>    예시(account_type은 '연금저축'/'퇴직연금', channel은 '온라인'/'오프라인'/
>    '온라인슈퍼')를 넣어줘. 실제 값을 안 보여주면 엉뚱한 리터럴을 만들어.
>
> 3. 안전장치:
>    - `SELECT`로 시작하는 쿼리만 허용. INSERT/UPDATE/DELETE/DROP/ATTACH 등은 거부
>    - 세미콜론으로 여러 문장 넣는 것 차단
>    - `LIMIT`이 없으면 자동으로 `LIMIT 50` 추가
>    - 실행 실패하면 에러 메시지를 붙여 1회만 재시도, 그래도 실패하면 검색 결과로 폴백
>
> 4. SQL 결과를 `state["sql_rows"]`에 넣고, `compose()`가 근거 청크와 **함께**
>    프롬프트에 넣도록 해. SQL 결과만으로 답하면 출처 표기가 빈약해져.
>
> 5. `to_response()`의 `retrieved_context`에 실행한 SQL과 결과를 포함시켜.
>    평가에서 이 필드가 채점 대상이라 SQL을 보여주는 게 유리해.
>
> 스키마는 `sqlite3 dataset/fund_fees.sqlite ".schema fund_fees"`로 확인해.
>
> 완료 후 `evalset_v1.json`의 상품 필터링 5문항(Q-013~Q-017)으로 검증해줘.
> 이 문항들이 Fee-SQL 없이는 못 푸는 것들이야.

---

## 작업 3 — FastAPI `/answer` 서버

> `agent.py`를 감싸는 FastAPI 서버 `server.py`를 만들어줘.
> 주최측 평가 API 규격은 `CLAUDE.md` 6절에 있어. 규격이 틀리면 답변 품질과
> 무관하게 0점이라 정확히 맞춰야 해.
>
> 요구사항:
>
> 1. `GET /answer?question_id=&question=` — 경로 고정, **인증 헤더를 요구하지 마**.
>    주최측은 헤더를 아무것도 안 보내. 미들웨어로 헤더 검증을 걸면 전부 실패해.
>
> 2. 응답 5개 필드 전부 문자열. `agent.to_response()`가 이미 그 형식이야.
>    반환 전에 5개 필드가 다 있고 전부 `str`인지 한 번 더 검증해.
>
> 3. 기동 시 `agent.warmup()` 호출. BM25 72MB 로딩에 10초쯤 걸리는데
>    첫 요청 안에서 하면 그 요청만 데드라인 예산을 까먹어.
>
> 4. 어떤 예외가 나도 **500을 내지 마.** 200에 부분 답변을 담아 반환해.
>    5xx가 나면 주최측이 재시도를 쓰고, 재시도 2회를 소진하면 그 문항은 0점이야.
>    최소한 "제공된 자료에서 확인되지 않습니다"라도 200으로 돌려줘.
>
> 5. `question_id` 캐싱 — `agent.py`에 이미 `_CACHE`가 있어. 재시도가 와도
>    같은 답을 주고 API 호출도 아껴.
>
> 6. `/health` 엔드포인트 추가 (인덱스 로딩 여부 반환). 배포 후 점검용.
>
> 7. 동시 요청은 고려 안 해도 돼. 주최측이 순차로 1건씩 보낸다고 명시했어.
>
> 만든 뒤 로컬에서 띄우고 이걸로 검증해줘:
>   `python eval_answers.py --endpoint http://127.0.0.1:8000/answer --evalset evalset_v2.json`
> 이 명령이 정확도뿐 아니라 응답 규격(필드 5개, 문자열 여부, Content-Type,
> 응답시간)까지 자동 점검해.

---

## 작업 4 — 배포 설정 파일

> NCP 서버 배포용 설정 파일을 만들어줘. 아직 서버가 없어도 파일은 미리 준비해두자.
>
> 1. `deploy/nginx.conf` — 80 → 127.0.0.1:8000 프록시.
>    `proxy_read_timeout`과 `proxy_send_timeout`을 **320초 이상**으로.
>    기본값 60초면 nginx가 먼저 끊어서 원인 못 찾고 헤맨다.
>
> 2. `deploy/pension-agent.service` — systemd 유닛.
>    `Restart=always`, `RestartSec=5`. 장애 시 재시도 2회 안에 살아나야 점수를 받아.
>    `WorkingDirectory`와 `EnvironmentFile`(.env 경로)도 지정.
>
> 3. `deploy/README.md` — 배포 절차를 순서대로.
>    NCP 공인 IP 할당 → ACG에서 80 인바운드 허용 → 파일 배치 → systemd 등록 →
>    `eval_answers.py --endpoint`로 검증. 각 단계에서 확인할 것도 적어줘.
>
> 4. 프로젝트 루트 `README.md`에 엔드포인트 주소 명시란을 만들어줘.
>    주최측이 README.md에 엔드포인트를 적으라고 요구했어(필수).

---

## 작업 5 — Verifier 노드 (시간이 남으면)

> 답변 속 숫자가 근거에 실제로 있는지 검사하는 Verifier 노드를 추가해줘.
> **LLM에게 "검증해줘"라고 부탁하지 마.** 코드로 문자열 대조해야 해.
> 수수료율을 지어내면 그 순간 서비스가 끝나는 도메인이야.
>
> 1. 답변에서 숫자 패턴을 추출 (금액, %, 배수, 기간)
> 2. 각 숫자가 `state["evidence"]`의 텍스트나 `state["sql_rows"]`에 있는지 대조
> 3. 없는 숫자가 있으면 그 부분을 표시해서 Composer로 1회만 재생성 요청
> 4. 재생성 후에도 없으면 해당 수치에 "(확인 불가)" 표기
> 5. 검증 과정을 `think_trace`에 남겨. 평가에서 이 필드가 채점 대상이야.
>
> 표기 흔들림을 흡수해야 해: "900만원" / "900만 원" / "9,000,000" / "900만"
> 은 같은 값으로 봐야 하고, 근거의 "0.72"와 답변의 "0.72%"도 매칭돼야 해.
>
> 완료 후 평가셋 두 개로 재측정해서 정확도가 떨어지지 않았는지 확인해줘.
> Verifier가 과하게 작동하면 맞는 답까지 "확인 불가"로 만들 수 있어.
